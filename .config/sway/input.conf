input type:pointer {
    accel_profile flat
    pointer_accel 0
    natural_scroll disabled
}

input type:keyboard {
    repeat_delay 200 
    repeat_rate 30 
    xkb_layout us,ru
    xkb_options grp:caps_toggle
}
